<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{path('aoshidoweb_homepage')}}">
                <span class="glyphicon glyphicon-leaf green" aria-hidden="true"></span>
                Myosotis 
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if is_granted('ROLE_USER') %}
                    <li class="dropdown" >
                        <a data-toggle="dropdown" role="button" aria-expanded="false" class="dropdown-toggle">
                            <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span> ABMs
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{path('abms_preguntas')}}"  data-parent="#MainMenu">Preguntas</a></li>
                            <li><a href="{{path('abms_temas')}}"  data-parent="#MainMenu">Temas</a></li>
                            <li><a href="{{path('abms_materias')}}"  data-parent="#MainMenu">Materias</a></li>
                            <li><a href="{{path('abms_carreras')}}"  data-parent="#MainMenu">Carreras</a></li>
                        </ul>
                    {% else %}

                    {% endif %}
                </li> 
                <li class="dropdown" >
                    <a data-toggle="dropdown" role="button" aria-expanded="false" class="dropdown-toggle">
                        <span class="glyphicon glyphicon-th" aria-hidden="true"></span> Games
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="" {% block cards %} {% endblock %} >
                            <a href="{{path('games_cards')}}">
                                <span class="glyphicon glyphicon-th-large" aria-hidden="true"></span> Cards
                            </a>
                        </li>
                        <li class="disabled" {% block quiz %} {% endblock %} >
                            <a href="#{#path('games_quiz')#}">
                                <span class="glyphicon glyphicon-tasks" aria-hidden="true"></span> Quiz
                            </a>
                        </li>
                        <li class="disabled" {% block challenge %} {% endblock %} >
                            <a href="#">
                                <span class="glyphicon glyphicon-tower" aria-hidden="true"></span> Challenge
                            </a>
                        </li>
                    </ul>
                </li>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li class="" {% block bugs %} {% endblock %} >
                        <a href="{{path('aoshidoweb_bugs')}}">
                            <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>           
                            Bug Tracker
                        </a>
                    </li>
                {% else %}
                    <li class="" {% block registration %} {% endblock %} ><a href="{{ path('fos_user_registration_register') }}">
                            <span class="glyphicon glyphicon-user" aria-hidden="true"></span> Registrate!
                        </a>
                    </li>
                {% endif %}
            </ul>


            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.user.username }} <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_show') }}">Perfil</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}">Cambiar contraseña</a></li>
                                {% if is_granted('ROLE_ADMIN') %}
                                <li><a href="{{ path('users_index') }}">Users ABM</a></li>
                                {% endif %}
                            <li class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                {{ render(controller('FOSUserBundle:Security:login')) }}

                {#<form class="navbar-form form-inline pull-right" action="{{ path('fos_user_registration_register') }}" method="post">
                    <div class="form-group">
                        <input class="btn btn-info" type="submit" id="_submit" name="_submit" value="Registrate" />
                    </div>
                </form>#}
            {% endif %}

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>

