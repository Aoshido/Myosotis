<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" >Aoshido</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li {% block home %} {% endblock %} ><a href="{{path('aoshidoweb_homepage')}}">
                        <span class="glyphicon glyphicon-home" aria-hidden="true"></span> Home
                    </a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="dropdown" >
                        <a data-toggle="dropdown" role="button" aria-expanded="false" class="dropdown-toggle" href="#{#path('abms_index')#}">
                            <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span> ABMs
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{path('abms_preguntas')}}"  data-parent="#MainMenu">Preguntas</a></li>
                            <li><a href="{{path('abms_temas')}}"  data-parent="#MainMenu">Temas</a></li>
                            <li><a href="{{path('abms_materias')}}"  data-parent="#MainMenu">Materias</a></li>
                            <li><a href="{{path('abms_carreras')}}"  data-parent="#MainMenu">Carreras</a></li>
                        </ul>
                    {% else %}

                    {% endif %}
                </li> 
                <li class="disabled" {% block quiz %} {% endblock %} ><a href="#">
                        <span class="glyphicon glyphicon-tasks" aria-hidden="true"></span> Quiz!
                    </a>
                </li>
                <li class="disabled" {% block cards %} {% endblock %} ><a href="#">
                        <span class="glyphicon glyphicon-th-large" aria-hidden="true"></span> Cards
                    </a>
                </li>
            </ul>

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.user.username }} <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_show') }}">Perfil</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}">Cambiar contraseña</a></li>
                            <li class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                            {{ render(controller('FOSUserBundle:Security:login')) }}
                {#<ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Ingresa! <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                        </ul>
                    </li>
                </ul>#}
            {% endif %}

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>

